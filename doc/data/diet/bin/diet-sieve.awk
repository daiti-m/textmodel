#!/usr/local/bin/gawk -f
BEGIN {
    FS = "\t";
}

# (NF >= 2) && ($1 !~ /委員長/) && ($1 !~ /大臣/) && ($1 !~ /長官/) && ($1 !~ /参考人/) {
(NF >= 2) && ($1 !~ /委員長/) && ($1 !~ /長官/) && ($1 !~ /参考人/) {
    if (length($2) >= 50) {
	print;
    }
}

